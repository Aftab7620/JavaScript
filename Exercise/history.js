

    
        document.write(history.length);
        function onback() {
            history.back();
        }
        function onforward() {
                    history.forward();
                }

        function goes(){
            history.go(-1);
        }

